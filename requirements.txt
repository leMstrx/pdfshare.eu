Flask==2.2.3          # Core web framework
Flask-Cors==3.0.10    # Enable Cross-Origin Resource Sharing
requests==2.28.2      # For making HTTP requests (if needed)
python-dotenv==0.21.0 # For .env file support (configuration)
gunicorn==20.1.0      # Production WSGI server (optional for deployment)
